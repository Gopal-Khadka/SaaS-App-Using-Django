name: 1 - Hello World Github Action


on:
  workflow_dispatch:
  # schedule:
  #   - cron: '30 15 * * *' # run cron job at 3pm 15mins everyday everyweek everymonth
   
  # push: # git push
  #   branch:
  #     - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello there
        run: echo "hello world"
    