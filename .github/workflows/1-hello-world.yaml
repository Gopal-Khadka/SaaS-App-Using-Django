name: 1 - Hello World Github Action


on:
  workflow_dispatch:
  push: # git push
    branch:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello there
        run: echo "hello world"
    